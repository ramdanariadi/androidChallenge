<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/search_action_menu"
        android:icon="@drawable/ic_action_search"
        android:title="@string/search_action_menu"
        app:showAsAction="always|collapseActionView"
        app:actionViewClass="android.support.v7.widget.SearchView"/>

    <item
        android:id="@+id/capture_image"
        android:icon="@drawable/ic_add_a_photo"
        android:title="@string/capture_image"
        app:showAsAction="always"/>

    <item
        android:id="@+id/setting_item"
        android:title="@string/setting_menu"
        app:showAsAction="never"/>

    <item
        android:id="@+id/help_item"
        android:title="@string/help_menu"
        app:showAsAction="never"/>
</menu>